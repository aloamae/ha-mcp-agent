# Configuration Claude — Projet MCP

Ce projet utilise **Claude Code** avec des serveurs **MCP**.

## 🔗 MCP configurés
- Home Assistant via `@coolver/home-assistant-mcp`

## 🔐 Variables d’environnement requises
- `HA_AGENT_URL`
- `HA_AGENT_KEY`

Ces variables doivent être définies dans un fichier `.env`
(non versionné).

## ▶️ Démarrage
Lancer Claude Code depuis la racine du projet :

```bash
claude
